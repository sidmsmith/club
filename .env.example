# Copy this file to `.env.local` in the same folder (club/) and fill in values.
# `.env.local` is gitignored — never commit secrets.
#
# From the club folder:
#   npm run test:lobby

NEON_DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
ABLY_API_KEY=
